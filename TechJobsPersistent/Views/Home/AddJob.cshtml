@model TechJobsPersistent.ViewModels.AddJobViewModel


<h1>Add a Job</h1>

<form asp-controller="Home" asp-action="ProcessAddJobForm" method="post">

    <div class="form-group">
        <label asp-for="Name"></label>
        <input class="form-control" asp-for="Name" />
        <span asp-validation-for="Name"></span>
    </div>

    <div class="form-group">
        <label asp-for="EmployerId">Employer</label>
        <select asp-for="EmployerId" asp-items="Model.Employers"></select>
        <p><a asp-controller="Employer" asp-action="Add">Add Employer</a></p>
    </div>

    @*<div class="form-group">
            <label asp-for="SkillId">Skills</label>
            <select asp-for="SkillId" asp-items="Model.Skill"></select>
            <p><a asp-controller="Skill" asp-action="Add">Add Skill</a></p>
        </div>*@


    @*@foreach (var skill in Model.Skill)
    {
        <div class="form-group">
            <label>
                <span>@skill</span>
                <input type="checkbox" name="skillIds" value="skill.Id" class="form-control">
            </label>
        </div>
     }*@



    @foreach (var skill in ViewBag.skills)
    {
        <div class="form-group">
            <label>
                <span>@skill.Name</span>
                <input type="checkbox" name="selectedSkills" value="@skill.Id" class="form-control">
            </label>
        </div>

        }


    <p><a asp-controller="Skill" asp-action="Add">Add Skill</a></p>

    <input type="submit" value="Add Job" />
</form>


